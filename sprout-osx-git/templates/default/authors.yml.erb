# This file is managed by sprout

authors:
<% node['sprout']['git']['authors'].each do |author| %>
  <%= "#{author["initials"]}: #{author["author"]}"%><%= author["username"] ? "; #{author["username"]}" : "" %>
<% end %>
email:
  domain: <%= node['sprout']['git']['domain'] %>
email_addresses:
<% node['sprout']['git']['authors'].each do |author| %>
<% if author['email'] %>
  <%= "#{author["initials"]}: #{author["email"]}"%>
<% end %>
<% end %>
